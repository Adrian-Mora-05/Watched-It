import { View, Text, ScrollView, TouchableOpacity } from 'react-native';

export default function ReviewDetailScreen() {
  return (
    <View className="flex-1">
      <Text className="text-large text-white">Review Detail</Text>
    </View>
  );
}